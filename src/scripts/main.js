// let conjunction = function (firstWord, secondWord) {
//     let conjoinedWord = `${firstWord} ${secondWord}`
//     console.log(conjoinedWord)
// }

// conjunction("Master", "Card")


// Thought the original code I edited wasn't working so I re-wrote it as a big arrow function
// Link your damn JS in the html, Cassie! 

const conjunction = (firstWord, secondWord) => {
const conjoinedWord = `${firstWord} ${secondWord}`
console.log(conjoinedWord)
}

conjunction("Master", "Card")